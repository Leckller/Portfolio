import styled from 'styled-components';

export const StyledApresentation = styled.section`
    display: flex;
    align-items: center;
    gap: 24px;
    
    img {
        max-width: 200px;
        border-radius: 9999px;
        border: 4px solid black;
    }

    article {
        border: 2px solid black;
        padding: 8px;
    }

`;
